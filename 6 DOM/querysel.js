let div = document.querySelector("div");
console.dir(div); // We use this tag to get first element

// let all_elements = document.querySelectorAll(".myname");
// console.dir(all_elements); // It return the list of Node. // // We use this tag to get all element


// let id_elements = document.querySelectorAll("#heading");
// console.dir(id_elements); // It return the list of Node. //


// let tag_elements = document.querySelectorAll("button");
// console.dir(tag_elements); // It return the list of Node. //  

let heading = document.querySelector("h1");
console.dir(heading);
