
let cartTotal = 45;  // Change this value to test different scenarios

let shippingCost = cartTotal >= 50 ? "Free Shipping" : "$5 Shipping Fee";

console.log(shippingCost);


let age = 15;

let result = (age>=18) ? "Adult" : "Not Adult"

console.log (result);